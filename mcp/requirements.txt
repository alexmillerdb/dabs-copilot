# MCP Server Dependencies for Databricks Apps Deployment
# This file contains only the runtime dependencies needed for the MCP server

# Databricks SDK and Core Dependencies
databricks-sdk==0.65.0
python-dotenv==1.0.1

# FastAPI and Web Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# MCP Server Framework
mcp>=1.12.4
httpx==0.28.1

# Configuration and Data Handling
pyyaml>=6.0.1

# Optional: Enhanced MCP clients for testing
databricks-mcp>=0.2.1

# Phase 2: DAB Generation Dependencies
astunparse>=1.6      # Python AST manipulation
sqlparse>=0.4        # SQL parsing for notebook analysis
jinja2>=3.1          # Template engine for bundle generation
jsonschema>=4.0      # Schema validation for bundles

# Testing and Development
pytest>=7.0          # Testing framework
pytest-asyncio>=0.21 # Async test support
pytest-mock>=3.10    # Mocking support
black>=23.0          # Code formatting
ruff>=0.1            # Linting
